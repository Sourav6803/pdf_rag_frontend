import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['www.pdfgear.com', 'www.w3.org', 'www.chatpdf.com'], // Add your hostname here
  },
};

export default nextConfig;
